command: ["streamlit", "run", "app.py", "--server.port=8080"]

env:
  - name: WORKSPACE_HOST
    valueFrom:
      hostUrl: ""

resources:
  requests:
    cpu: "2"
    memory: "4Gi"
  limits:
    cpu: "4"
    memory: "8Gi"
