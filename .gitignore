*.csv
venv/
.venv/
graphify-out/
*.pyc
.mypy_cache/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
mutants/
# Deploy branch: the full-size artifacts are replaced by movies_final.parquet and
# tfidf_matrix.npz (see src/data/build_deploy_assets.py). Keeping the .pkl out of
# this branch is what lets it stay free of Git LFS, which Streamlit Cloud cannot read.
models/tfidf_matrix.pkl
